# systemd service definition for the SPEP Daemon that allows for multiple named instances.
# There should also be an spepd.service definition that for a single non-named instance (which can be used in parallel with this service definition)

[Unit]
Description=SPEP Daemon %I
After=network.target

[Service]
ExecStart=${SPEP_HOME}/sbin/spepd -l /var/log/spepd/spepd-%I.log -f ${SPEP_HOME}/etc/spep/spep-%I.conf -p /run/spepd/spepd-%I.pid
Type=forking
PIDFile=/run/spepd/spepd-%I.pid
User=spepd

[Install]
WantedBy=default.target
